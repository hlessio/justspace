# What this isn't

- Not a design system (it's an engine)
- Not responsive breakpoints (it's continuous)
- Not a component library (it's one recursive component)
- Not a state machine (there are no states)
- Not a page router (there are no pages)

It's a weight field. That's it.
