# The cursor is a field, not a pointer

Your mouse doesn't "select" things. It deforms space around itself. Like gravity.

Every element responds based on distance from the cursor. Gaussian falloff. No binary hover/not-hover.

Move your mouse slowly across the screen. Watch the space breathe.
