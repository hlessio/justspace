# Adding a feature = adding a node

Want a calendar? Add a calendar node.
Want a kanban? Add a kanban node.
Want notes? They're already nodes.

Every feature lives in the same space. Same physics. Same weight. Same expansion. The engine doesn't know what's inside — it just knows weight and children.
